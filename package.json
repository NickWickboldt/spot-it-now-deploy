{
  "name": "spotitnow",
  "private": true,
  "scripts": {
    "dev:frontend": "cd frontend && npx expo start -c",
    "dev:backend": "cd backend && node src/server.js",
    "dev": "concurrently --raw \"npm run dev:frontend\" \"npm run dev:backend\""
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  },
  "dependencies": {
    "expo-av": "^16.0.7",
    "expo-image-manipulator": "^14.0.7",
    "expo-video-thumbnails": "^10.0.7"
  }
}
