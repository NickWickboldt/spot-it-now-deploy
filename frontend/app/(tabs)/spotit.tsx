import { View, Text, StyleSheet } from 'react-native';
import { tabStyles } from '../../constants/TabStyles';

export default function SpotItScreen() {
  return (
    <View style={tabStyles.container}>
      <Text style={tabStyles.title}>Spot It Page</Text>
    </View>
  );
}
